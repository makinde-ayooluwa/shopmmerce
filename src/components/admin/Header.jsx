export default function AdminHeader() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "space-between",
        borderBottom: "2px solid",
        borderColor: "#000",
        padding: "10px",
      }}
    ></div>
  );
}
